services:
  - type: web
    name: stock-dashboard
    runtime: python
    buildCommand: pip install -r render_requirements.txt
    startCommand: uvicorn viewer_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000
